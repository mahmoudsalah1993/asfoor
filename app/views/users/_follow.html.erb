<br><br><br>
<%= form_for(current_user.active_requests.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :requested_id, @user.id %></div>
  <%= f.submit "Friend Request", class: "btn btn-primary" %>
<% end %>
